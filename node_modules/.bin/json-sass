#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../json-sass/lib/bin/json-sass" "$@"
  ret=$?
else 
  node  "$basedir/../json-sass/lib/bin/json-sass" "$@"
  ret=$?
fi
exit $ret
