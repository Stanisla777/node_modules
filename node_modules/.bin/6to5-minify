#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babel/bin/deprecated/6to5-minify" "$@"
  ret=$?
else 
  node  "$basedir/../babel/bin/deprecated/6to5-minify" "$@"
  ret=$?
fi
exit $ret
